# Task 12 — Architecture notes + handoff docs

## Objective
Document the new module boundaries and how to debug the sidepanel + agent loop flows.

## Files Involved
- `work/sidepanel-modularization-and-agent-loop-cleanup/` (notes)
- Optional: `README` or `docs/` (if needed)

## Changes
- Add a short architecture note in the workpack or repo docs:
  - Module map (settings/auth/chat/tools/history/tabs/agents/runtime).
  - Entry points (`sidepanel/panel.ts`, `background.ts`, `ai/agent-loop.ts`).
  - How to debug runtime message order.

## Tests
- None (documentation-only).

## Validation
- N/A

## Acceptance Criteria
- Clear handoff notes exist for the new module layout.